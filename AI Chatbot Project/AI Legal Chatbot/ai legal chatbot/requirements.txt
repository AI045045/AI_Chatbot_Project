# AI Legal Chatbot - Python Requirements
# Lists the package dependencies required to run the local legal chatbot application.

streamlit>=1.28,<2.0

# LangChain RAG components (local Ollama + in-memory vector index)
langchain>=0.2,<0.4
langchain-community>=0.2,<0.4
langchain-text-splitters>=0.2,<0.4

# In-memory vector search (prebuilt wheels on Windows; avoids Chroma's chroma-hnswlib C++ build)
faiss-cpu>=1.8,<2.0

# Ollama integration is provided by langchain-community; Ollama itself must be installed separately.

# Document loaders
pypdf>=4.0,<6.0
python-docx>=1.0,<2.0

# Auth: bcrypt (SHA-256 pre-hash removes the classic 72-byte bcrypt password limit)
bcrypt>=4.0,<6.0

